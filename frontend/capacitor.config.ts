import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fastcash.app',
  appName: 'Fast Cash',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
